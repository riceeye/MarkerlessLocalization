# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: /Users/timli/MarkerlessLocalization/mask_generation/mask_3 # dataset root dir (absolute or relative; if relative, it's relative to default datasets_dir)
# run top from the mask_3 folder
train: ./new_data/training # train images (relative to 'path') 4 images
val: ./new_data/validation # val images (relative to 'path') 4 images
test: ./new_data/testing # test images (optional)

# Classes (80 COCO classes)
names:
    0: red cube
